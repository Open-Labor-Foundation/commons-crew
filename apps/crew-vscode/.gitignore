node_modules/
dist/
*.vsix
package-lock.json
# governance/ is copied from ../../governance at build time (packaged into the
# vsix, never committed here).
governance/
# catalog/platform-assistant/ is copied from ../../catalog/platform-assistant
# at build time, same reason.
catalog/
